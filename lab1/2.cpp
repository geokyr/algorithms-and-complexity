#include <iostream>
using namespace std;

#define N 20000
int n, k, residents[N];

int main() {
    ios_base::sync_with_stdio(false);

    cin >> n >> k;
    
    for (int i = 0; i < n; i++) {
        cin >> residents[i];
    }

    

    return 0;
}